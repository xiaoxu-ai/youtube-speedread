# 油管速读 YT SpeedRead — 使用说明

在 YouTube 视频页点一下，你会拿到一篇可以直接读的中文稿，分三部分：

1. **视频简介** —— 全片总述 +（访谈/演讲类还会有）人物介绍 + 要点清单
2. **主题拆解** —— 按话题切分（约每 15 分钟一个，3～10 个），每个话题给出「讲了什么 / 要点」，标题带可点的时间区间
3. **双语全文** —— 原文 + 中文逐段对照，每段带可点的时间戳
   （视频原声本来就是中文时，这节是「全文」单栏 —— 不会花钱去做中译中）

文章开头会列出：频道、原视频链接、**发布时间**（判断内容时效性用）、
视频时长、字幕语言、字幕长度、提炼时间。

可一键复制或下载成 Markdown。

> **长视频可以处理。** 处理过程中会逐块保存同一浏览器会话内的检查点；完整退出浏览器、
> 扩展重载或更新后，恢复不保证可用。

---

## 第一步：安装

1. 打开扩展管理页
   - Chrome：`chrome://extensions/`
   - Brave：`brave://extensions/`
2. 右上角打开 **开发者模式**
3. 点 **加载已解压的扩展程序**
4. 选择你下载或克隆的项目文件夹中的 `extension/` 子文件夹
5. 工具栏会出现插件图标

> 如果之前装过旧版（「YouTube 字幕提取器」），先把它**移除**再装新的，避免两个插件同时抢字幕。

---

## 第二步：配置 AI 接口

插件自己不翻译，需要接一个大模型。支持所有 **OpenAI 兼容**接口。

1. 右键插件图标 → **选项**（或在弹窗里点「⚙ 设置」）
2. 填三项：

| 字段 | 填什么 |
|---|---|
| **API Base URL** | 填到 `/v1` 为止，比如 `https://api.deepseek.com/v1` |
| **API Key** | 你的 Key |
| **模型名** | 比如 `deepseek-v4-flash` |

3. 点 **保存设置**
4. **浏览器会弹窗问是否允许访问该域名 —— 必须点「允许」。**
   不给这个权限，调用接口会被浏览器以跨域为由拦掉。
5. 点 **测试连接**，看到「连接正常」就成了

常见接口：

| 服务商 | Base URL | 模型示例 |
|---|---|---|
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-v4-flash` |
| Moonshot | `https://api.moonshot.cn/v1` | `moonshot-v1-32k` |
| z.ai（国际版） | `https://api.z.ai/api/paas/v4` | `glm-5.1` |
| 智谱（国内） | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` |

---

## 第三步：用

1. 打开一个**带字幕**的 YouTube 视频页（网址里有 `/watch`）
2. 点插件图标 → **开始速读**
3. 等着。**这个弹窗可以随手关掉，任务在后台继续跑**，不影响结果
   - **YouTube 标签页**：等进度条开始动（显示「x/y 块」）之后也可以关掉
4. 完成后会自动开一个新标签页显示结果

在结果页可以 **复制 Markdown** 或 **下载 .md**。
下载的文件统一带 `YTSR_` 前缀，方便在下载文件夹里认出和排序。
双语正文里每段的时间戳都是链接，点一下直接跳到视频对应位置。

> **注意：抓字幕时视频会被静音播放几秒。** 这是拿到 YouTube 字幕授权令牌的必要动作，
> 抓完插件会把播放进度、音量、字幕开关都恢复原样。

---

## 参数说明

在设置页的「翻译参数」里：

| 参数 | 默认 | 说明 |
|---|---|---|
| **Temperature** | 0.3 | 0 最死板，1 以上开始发挥。翻译建议 0～0.5 |
| **单次最大输出 tokens** | 8192 | 不够时插件会自动加大重试。DeepSeek 默认只有 4096 |
| **每块字符数** | 2400 | 字幕切成多小一块去翻译。调小更稳、调大更快 |
| **并发块数** | 3 | AI 请求并发数；检查点写入已单独排队，不需要为防覆盖而降到 1。遇到 429 再降低 |
| **优先字幕语言** | 自动 | 视频有多条字幕轨道时用哪条。「自动」会选视频的**原声语言**那条 |

### Temperature 怎么选

| 值 | 效果 | 适合 |
|---|---|---|
| 0 | 最保守，每次输出都差不多 | 机械直译、事实提取 |
| 0.3～0.5 | 稳定但读着顺 | **字幕翻译（推荐）** |
| 1.0 | 比较平衡 | 写作、头脑风暴 |
| 1.5+ | 很发散，容易胡说 | 不建议用在翻译上 |

---

## 常见问题

**Q：提示「字幕接口返回空内容」/「没能拿到有效的 pot 令牌」？**
A：YouTube 要求先由播放器自己发起一次字幕请求。通常插件会自动完成这一步；
如果失败，**手动点一下播放器上的 CC 按钮把字幕显示出来，然后重试**。

**Q：提示「该视频没有字幕轨道」？**
A：这个视频既没有上传字幕，也没有自动字幕，没办法处理。换一个有 CC 的视频。

**Q：提示「播放器上找不到字幕(CC)按钮」？**
A：页面还没加载完，或者不是标准播放页。刷新页面等播放器出来再试。

**Q：有段落显示「本段翻译缺失」？**
A：当前源码已经修复整块检查点被并发覆盖的问题，并会在整块缺失时明确报错。
单个段落仍可能因为模型没按编号格式输出而缺失；插件会自动重试并用多层解析兜底。
如果仍出现，把「每块字符数」调小到 1500。原文始终保留。

**Q：报错「AI 接口余额不足」？**
A：字面意思，去你的服务商后台充值或换一个 Key。
注意有些服务商（比如 z.ai）把「余额不足」混在 HTTP 429 里返回，看起来像限流，其实不是。

**Q：报错 429 / 限流？**
A：把「并发块数」调到 1。

**Q：报错「跨域权限没给」/ 请求失败？**
A：回设置页重新点一次「保存设置」，在弹窗里选**允许**。

**Q：报错里出现 `404 NOT_FOUND` 或「接口没返回 choices」？**
A：Base URL 填错了（路径不对）。到设置页点 **「测试连接」** ——
它会自动把同域名下的常见路径挨个试一遍，找到对的会直接帮你填进输入框，然后点「保存设置」。

> 踩过的坑：z.ai 国际版正确地址是 `https://api.z.ai/api/paas/v4`。
> 网上流传的 `https://api.z.ai/api/openai/v1` 是**失效的**，它会返回
> HTTP 200 + `{"code":500,"msg":"404 NOT_FOUND"}`，看起来像模型问题，其实是地址问题。

**Q：点了没反应 / 卡在某个进度？**
A：先等一会儿 —— 长视频本来就慢，2 小时的视频要跑五到十分钟。
如果进度条真的不动了，弹窗上会出现「从断点继续」按钮，点它就行（已完成的部分不会重跑）。
实在不行就点弹窗底部的「清除任务状态」再重来。

**Q：处理到一半我关了浏览器 / 电脑睡着了？**
A：同一浏览器会话内，插件会尽力从已保存的检查点恢复。完整退出浏览器、扩展重载、更新或禁用后，
session 检查点可能已清除，此时需要重新开始。

**Q：视频怎么自己播起来了？**
A：正常的，抓字幕必须让播放器跑一下。抓完会恢复原状。

**Q：模型名填什么？**
A：优先选择你的服务商当前支持、适合翻译且上下文长度足够的非推理模型。模型名称、价格和可用性
会变化，请以服务商当前文档和设置页的连接测试为准。

**Q：「人物介绍」为什么有时候没有？**
A：只有访谈、对谈、演讲、播客这类有明确讲话人的视频才会写。教程、评测、新闻播报这种
没有人物主体的会自动跳过。另外如果 YouTube 页面的简介栏和开场白里都查不出这个人是谁，也会跳过 ——
**宁可不写也不猜**，编错别人的身份比不写更糟。

**Q：能同时跑两个视频吗？**
A：**同一个浏览器不行**，会提示「已有任务在跑」，得等前一个跑完。
**Brave 和 Chrome 可以同时各跑一个**，两者完全独立。
但它们共用你的 API Key，并发翻倍容易被限流——真要这么用就把「并发块数」调到 1～2。

**Q：能离线用吗？**
A：不能，翻译要联网调 AI 接口。

---

## 换图标

内置多套图标方案，当前用的是 **A3（纸白 / 朱红）**。

- **A1~A6** 同一个「播放键化作文字行」的造型，六种配色
- **B** 摊开的书 + 播放键
- **C** 字幕框 +「读」

```bash
cd extension/icons
./build.sh --list   # 看所有方案
./build.sh A5       # 换成 A5（正红 / 纯白）
```

然后回扩展管理页点一下插件的**刷新**按钮。
